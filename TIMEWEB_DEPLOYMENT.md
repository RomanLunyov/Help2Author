# 🚀 Развёртывание бота Help2Author на Timeweb Cloud

## 📋 Содержание
1. [Подготовка к развертыванию](#подготовка)
2. [Создание сервера на Timeweb](#создание-сервера)
3. [Подключение к серверу](#подключение)
4. [Установка необходимого ПО](#установка-по)
5. [Загрузка и настройка бота](#загрузка-бота)
6. [Запуск бота](#запуск)
7. [Настройка автозапуска](#автозапуск)
8. [Мониторинг и обслуживание](#мониторинг)

---

## 🎯 Подготовка к развертыванию {#подготовка}

### Что вам понадобится:

1. **Аккаунт на Timeweb Cloud**
   - Зарегистрируйтесь на https://timeweb.cloud/
   - Пополните баланс (минимум ~150-200₽/месяц для VPS)

2. **Токен Telegram бота**
   - Получите через [@BotFather](https://t.me/BotFather)
   - Команда: `/newbot` или используйте существующий токен

3. **Ваш Telegram ID**
   - Узнайте через бота [@userinfobot](https://t.me/userinfobot)
   - Это будет ID администратора

4. **SSH-клиент** (для Windows):
   - Встроенный PowerShell SSH (Windows 10+)
   - Или [PuTTY](https://www.putty.org/)

---

## 🖥️ Создание сервера на Timeweb Cloud {#создание-сервера}

### Шаг 1: Заказ VPS

1. Войдите в панель управления Timeweb Cloud
2. Перейдите в раздел **"Облачные серверы"** → **"Создать сервер"**

### Шаг 2: Выбор конфигурации

**Рекомендуемая конфигурация для бота:**

```
┌──────────────────────────────────────┐
│ Операционная система: Ubuntu 22.04   │
│ vCPU: 1 ядро                         │
│ RAM: 512 MB - 1 GB                   │
│ Диск: 10 GB SSD                      │
│ Стоимость: ~150-250₽/месяц           │
└──────────────────────────────────────┘
```

**Пошагово:**

1. **ОС**: Выберите `Ubuntu 22.04 LTS` (рекомендуется)
2. **Тариф**: 
   - Минимум: 512 MB RAM, 1 vCPU
   - Оптимально: 1 GB RAM, 1 vCPU
3. **Локация**: Выберите ближайший датацентр (Москва/СПб)
4. **SSH-ключ** (опционально, но рекомендуется):
   - Создайте или загрузите свой публичный SSH-ключ
   - Если пропустите - получите пароль root по email

5. **Имя сервера**: `help2author-bot` (или любое другое)
6. Нажмите **"Создать сервер"**

### Шаг 3: Получение данных для подключения

После создания сервера вы получите:
```
IP-адрес: 123.456.78.90
Пользователь: root
Пароль: (отправлен на email или используйте SSH-ключ)
```

**Сохраните эти данные!**

---

## 🔌 Подключение к серверу {#подключение}

### Способ 1: PowerShell (Windows 10/11)

Откройте PowerShell и выполните:

```powershell
ssh root@123.456.78.90
```

При первом подключении согласитесь добавить сервер в known_hosts:
```
Are you sure you want to continue connecting (yes/no)? yes
```

Введите пароль (при вводе символы не отображаются - это нормально).

### Способ 2: PuTTY (Windows)

1. Скачайте и установите [PuTTY](https://www.putty.org/)
2. Запустите PuTTY
3. Введите:
   - **Host Name**: `123.456.78.90`
   - **Port**: `22`
   - **Connection type**: `SSH`
4. Нажмите **Open**
5. Введите логин `root` и пароль

---

## 📦 Установка необходимого ПО {#установка-по}

После подключения к серверу выполните следующие команды:

### Шаг 1: Обновление системы

```bash
# Обновление списка пакетов
sudo apt update

# Обновление установленных пакетов
sudo apt upgrade -y
```

⏱️ Время выполнения: 2-5 минут

### Шаг 2: Установка Python 3.10+

```bash
# Установка Python и необходимых пакетов
sudo apt install python3 python3-pip python3-venv git -y

# Проверка версии Python
python3 --version
```

✅ Должна быть версия 3.10 или выше (в Ubuntu 22.04 это Python 3.10.x)

### Шаг 3: Установка дополнительных инструментов

```bash
# Установка screen (для фоновой работы бота)
sudo apt install screen -y

# Установка nano (текстовый редактор, если нужен)
sudo apt install nano -y
```

---

## 📥 Загрузка и настройка бота {#загрузка-бота}

### Вариант А: Загрузка через Git (если проект на GitHub/GitLab)

```bash
# Переход в домашнюю директорию
cd ~

# Клонирование репозитория (замените URL на свой)
git clone https://github.com/yourusername/Help2Author.git

# Переход в папку проекта
cd Help2Author
```

### Вариант Б: Загрузка файлов через SCP/SFTP (рекомендуется)

**На вашем локальном компьютере (Windows PowerShell):**

```powershell
# Упакуйте проект в архив
Compress-Archive -Path "c:\My telegram Bots\Help2Author\*" -DestinationPath "$env:USERPROFILE\Desktop\Help2Author.zip"

# Загрузите на сервер
scp "$env:USERPROFILE\Desktop\Help2Author.zip" root@123.456.78.90:~
```

**На сервере (в SSH-сессии):**

```bash
# Установите unzip если нужно
sudo apt install unzip -y

# Распакуйте архив
unzip Help2Author.zip -d ~/Help2Author

# Переход в папку проекта
cd ~/Help2Author
```

### Вариант В: Создать проект вручную

```bash
# Создание директории
mkdir -p ~/Help2Author
cd ~/Help2Author

# Создание структуры папок
mkdir handlers
```

Затем создайте все необходимые файлы, используя `nano` или другой редактор.

---

## ⚙️ Настройка окружения {#настройка-окружения}

### Шаг 1: Создание виртуального окружения

```bash
cd ~/Help2Author

# Создание виртуального окружения
python3 -m venv venv

# Активация виртуального окружения
source venv/bin/activate
```

После активации в начале строки должно появиться `(venv)`:
```
(venv) root@help2author-bot:~/Help2Author#
```

### Шаг 2: Установка зависимостей

```bash
# Установка зависимостей из requirements.txt
pip install -r requirements.txt
```

⏱️ Время установки: 1-3 минуты

### Шаг 3: Создание файла .env

```bash
# Создание .env файла
nano .env
```

Вставьте следующее содержимое (замените значения на свои):

```env
# Токен вашего бота (получите у @BotFather)
BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz

# Ваш Telegram ID (узнайте у @userinfobot)
ADMIN_ID=123456789

# Ссылка на чат для отзывов
FEEDBACK_CHAT_LINK=https://t.me/your_feedback_chat

# Реквизиты для поддержки (замените на свои)
SUPPORT_CARD_NUMBER_1=1234 5678 9012 3456
SUPPORT_CARD_NUMBER_2=9876 5432 1098 7654
SUPPORT_WALLET_1=ваш_кошелек_1
SUPPORT_WALLET_2=ваш_кошелек_2
SUPPORT_WALLET_3=ваш_кошелек_3
```

**Сохранение в nano:**
- Нажмите `Ctrl + X`
- Нажмите `Y` (да)
- Нажмите `Enter`

### Шаг 4: Проверка конфигурации

```bash
# Проверка, что .env файл создан
cat .env

# Проверка структуры проекта
ls -la
```

Должны увидеть все файлы проекта: `main.py`, `config.py`, `database.py`, и т.д.

---

## 🚀 Запуск бота {#запуск}

### Тестовый запуск

```bash
# Убедитесь, что виртуальное окружение активно
source venv/bin/activate

# Запуск бота
python3 main.py
```

✅ **Успешный запуск выглядит так:**
```
2025-12-27 10:30:45,123 - __main__ - INFO - Bot is starting...
2025-12-27 10:30:45,234 - __main__ - INFO - Database initialized
2025-12-27 10:30:45,345 - __main__ - INFO - Scheduler started
2025-12-27 10:30:45,456 - __main__ - INFO - Bot started successfully
```

🧪 **Проверьте бота:**
- Откройте Telegram
- Найдите вашего бота
- Отправьте команду `/start`
- Бот должен ответить меню

Если все работает, нажмите `Ctrl + C` для остановки.

---

## 🔄 Настройка автозапуска (Systemd) {#автозапуск}

Чтобы бот работал постоянно и автоматически запускался после перезагрузки сервера:

### Шаг 1: Создание systemd service

```bash
# Создание файла службы
sudo nano /etc/systemd/system/help2author-bot.service
```

Вставьте следующее содержимое:

```ini
[Unit]
Description=Help2Author Telegram Bot
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory=/root/Help2Author
Environment="PATH=/root/Help2Author/venv/bin"
ExecStart=/root/Help2Author/venv/bin/python3 /root/Help2Author/main.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

Сохраните файл (`Ctrl + X`, `Y`, `Enter`).

### Шаг 2: Активация и запуск службы

```bash
# Перезагрузка конфигурации systemd
sudo systemctl daemon-reload

# Включение автозапуска
sudo systemctl enable help2author-bot.service

# Запуск службы
sudo systemctl start help2author-bot.service

# Проверка статуса
sudo systemctl status help2author-bot.service
```

✅ **Служба работает, если видите:**
```
● help2author-bot.service - Help2Author Telegram Bot
   Loaded: loaded (/etc/systemd/system/help2author-bot.service; enabled)
   Active: active (running) since...
```

### Шаг 3: Управление службой

```bash
# Остановка бота
sudo systemctl stop help2author-bot.service

# Перезапуск бота
sudo systemctl restart help2author-bot.service

# Просмотр логов
sudo journalctl -u help2author-bot.service -f

# Просмотр последних 100 строк логов
sudo journalctl -u help2author-bot.service -n 100
```

---

## 📊 Мониторинг и обслуживание {#мониторинг}

### Проверка работы бота

```bash
# Проверка статуса службы
sudo systemctl status help2author-bot.service

# Просмотр логов в реальном времени
sudo journalctl -u help2author-bot.service -f

# Проверка процессов
ps aux | grep python

# Проверка использования ресурсов
top
```

### Резервное копирование базы данных

```bash
# Создание резервной копии БД
cp ~/Help2Author/books_bot.db ~/Help2Author/books_bot.db.backup-$(date +%Y%m%d)

# Или автоматическое резервное копирование (добавьте в crontab)
crontab -e
```

Добавьте строку для ежедневного бэкапа в 3:00:
```
0 3 * * * cp ~/Help2Author/books_bot.db ~/Help2Author/backups/books_bot.db.$(date +\%Y\%m\%d) && find ~/Help2Author/backups/ -mtime +30 -delete
```

Создайте папку для бэкапов:
```bash
mkdir -p ~/Help2Author/backups
```

### Обновление бота

```bash
# Остановка службы
sudo systemctl stop help2author-bot.service

# Переход в папку проекта
cd ~/Help2Author

# Резервное копирование
cp books_bot.db books_bot.db.backup

# Обновление кода (через git)
git pull

# Или загрузка новых файлов через SCP
# (выполните на локальном компьютере)
# scp файл.py root@123.456.78.90:~/Help2Author/

# Обновление зависимостей (если изменились)
source venv/bin/activate
pip install -r requirements.txt --upgrade

# Запуск службы
sudo systemctl start help2author-bot.service

# Проверка
sudo systemctl status help2author-bot.service
```

---

## 🔒 Безопасность

### Базовые настройки безопасности

```bash
# 1. Обновление системы
sudo apt update && sudo apt upgrade -y

# 2. Настройка файрвола
sudo ufw allow 22/tcp  # SSH
sudo ufw enable

# 3. Отключение входа root по паролю (используйте SSH-ключи)
sudo nano /etc/ssh/sshd_config
```

В файле измените:
```
PermitRootLogin prohibit-password
PasswordAuthentication no
```

Перезапустите SSH:
```bash
sudo systemctl restart sshd
```

### Защита .env файла

```bash
# Ограничение прав доступа
chmod 600 ~/Help2Author/.env
```

---

## 🆘 Решение проблем

### Бот не запускается

```bash
# Проверка логов
sudo journalctl -u help2author-bot.service -n 50

# Проверка синтаксиса Python
cd ~/Help2Author
source venv/bin/activate
python3 -m py_compile main.py

# Проверка .env файла
cat .env
```

### Бот не отвечает

```bash
# Проверка, что процесс запущен
ps aux | grep python

# Перезапуск
sudo systemctl restart help2author-bot.service

# Проверка токена бота
# Отправьте запрос к Telegram API:
curl https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getMe
```

### База данных заблокирована

```bash
# Остановка бота
sudo systemctl stop help2author-bot.service

# Проверка файла БД
ls -lh ~/Help2Author/books_bot.db

# Удаление lock-файла если есть
rm ~/Help2Author/books_bot.db-shm
rm ~/Help2Author/books_bot.db-wal

# Запуск
sudo systemctl start help2author-bot.service
```

---

## 📝 Полезные команды

```bash
# Подключение к серверу
ssh root@123.456.78.90

# Переход в папку проекта
cd ~/Help2Author

# Просмотр статуса бота
sudo systemctl status help2author-bot.service

# Просмотр логов
sudo journalctl -u help2author-bot.service -f

# Перезапуск бота
sudo systemctl restart help2author-bot.service

# Редактирование .env
nano .env
sudo systemctl restart help2author-bot.service

# Просмотр использования диска
df -h

# Просмотр использования памяти
free -h

# Очистка логов (старше 7 дней)
sudo journalctl --vacuum-time=7d
```

---

## 💡 Рекомендации

1. **Регулярные бэкапы**: Настройте автоматическое резервное копирование БД
2. **Мониторинг**: Проверяйте логи раз в несколько дней
3. **Обновления**: Обновляйте систему: `sudo apt update && sudo apt upgrade`
4. **Документация**: Сохраните данные доступа в безопасном месте
5. **Тестирование**: После обновления кода всегда проверяйте работу бота

---

## 🎉 Готово!

Ваш бот Help2Author теперь работает на Timeweb Cloud 24/7!

**Быстрая справка:**
- **Подключение**: `ssh root@123.456.78.90`
- **Перезапуск**: `sudo systemctl restart help2author-bot.service`
- **Логи**: `sudo journalctl -u help2author-bot.service -f`
- **Редактирование**: `nano ~/Help2Author/.env`

**Полезные ссылки:**
- [Панель Timeweb Cloud](https://timeweb.cloud/my/servers)
- [Документация Timeweb](https://timeweb.cloud/help/)
- [Telegram Bot API](https://core.telegram.org/bots/api)

---

*Если возникнут проблемы - проверьте логи и секцию "Решение проблем" выше.*
